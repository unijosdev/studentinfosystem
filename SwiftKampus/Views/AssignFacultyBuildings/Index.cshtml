@model IEnumerable<SwiftKampusModel.TimeTable.AssignFacultyBuilding>

@{
    ViewBag.Title = "Index";
}

<!-- Modal -->
<div class="modal fade modal-bf-fh" id="actionsList" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <!--Content-->
        <div class="modal-content">
            <!--Header-->
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
                <div class="flex-center">
                    <h4 class="modal-title">ASSIGN FACULTY TO BUILDING</h4>
                    @*<a class="btn btn-primary btn-sm">Create new action</a>*@
                </div>
            </div>
            <!--Body-->
            <div class="modal-body" id="myModalDivBody">
            </div>
        </div>
        <!--/.Content-->
    </div>
</div>
<!-- /.Modal-->

<div class="ibox float-e-margins">
    <div class="ibox-title">
        <h5>List of Assigned Building</h5>
        <div class="ibox-tools">
            <a href="#" onclick="myFunction()" class="btn btn-primary btn-sm"><i class="fa fa-plus left"></i> ASSIGN BUILDING</a>

            <a class="collapse-link">
                <i class="fa fa-chevron-up"></i>
            </a>
            <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                <i class="fa fa-wrench"></i>
            </a>

        </div>
        <div class="ibox-content">

            <table id="Jdatatable" class="table table-striped table-hover">
                <thead>
                    <tr>
                        <th>Faculty Name</th>
                        <th>Building Name</th>                       
                        <th>Action</th>
                    </tr>
                </thead>
            </table>
        </div>
    </div>
</div>


<script src="~/Scripts/jquery-3.1.1.min.js"></script>

<script type="text/javascript">
    $(document).ready(function () {

        window.oTable = $("#Jdatatable").DataTable({
            "processing": true, // for show progress bar while loading
            //"serverSide": true, // if it is true the preocess is done on server side
            //"filter": true, // disables search box
            //"info": true,
            //"stateSave": true,
            //"orderMulti": false, // disable multiple column loading at once
            "ajax": {
                "url": "/AssignFacultyBuildings/GetIndex",
                "type": "POST",
                "datatype": "json"
            },
            "columns": [
                { "data": "FacultyName", "autoWidth": true },
                { "data": "BuildingName", "autoWidth": true },
             
                {
                    "data": "AssignFacultyBuildingId",
                    "autoWidth": true,
                    "render": function (data) {
                        return '<a href="#" onclick="Edit(' + data + ')" >  <i class="fa fa-pencil" aria-hidden="true" >&nbsp;Edit</i></a>&nbsp;&nbsp;' +
                            '<a href="#" class="text-danger"  onclick="Delete(' + data + ')" ><i class="fa fa-remove" aria-hidden="true" >&nbsp;Remove<i /></a>';
                    }
                }

                //{ "data": "EmailID", "autoWidth": true },
                //{ "data": "Phone", "autoWidth": true },
            ]
        });

    });
    function Edit(assignFacultyBuildingId) {
        var url = "/AssignFacultyBuildings/Save?id=" + assignFacultyBuildingId;
        //window.alert(url);
        $('#myModalDivBody').load(url,
            function () {
                $('#actionsList').modal('show');
            });
    };
    function Delete(assignFacultyBuildingId) {
        var url = "/AssignFacultyBuildings/Delete?id=" + assignFacultyBuildingId;
        $('#myModalDivBody').load(url,
            function () {
                $('#actionsList').modal("show");
            });
    };
    function myFunction() {
        var url = "/AssignFacultyBuildings/Save?id=0";
        $('#myModalDivBody').load(url,
            function () {
                $('#actionsList').modal("show");
            });
    };
</script>

