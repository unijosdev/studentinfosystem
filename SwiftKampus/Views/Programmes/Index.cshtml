@model IEnumerable<SwiftKampusModel.Programme>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<div class="wrapper wrapper-content animated fadeInRight">
    <div class="row">
        <div class="col-lg-12">
            <div class="ibox float-e-margins">
                <div class="ibox-title">
                    <h4>List of Department Option</h4>
                    <div class="ibox-tools">
                        <a onclick="myFunction()" class="btn btn-primary" ><i class="fa fa-plus left"></i> ADD DEPT OPTION</a>
                        <a href="#" onclick="ExcelUpload()" class="btn btn-primary"><i class="fa fa-file-excel-o"></i> UPLOAD DEPT OPTION</a>
                        <a class="collapse-link">
                            <i class="fa fa-chevron-up"></i>
                        </a>
                        <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                            <i class="fa fa-wrench"></i>
                        </a>
                    </div>
                </div>


                <div class="ibox-content">
                    <div class="table-responsive">
                        <table id="Jdatatable" class="table table-striped table-bordered table-hover table-responsive dataTables-example">
                            <thead>
                            <tr>
                                <th>Department Name</th>
                                <th>Dept Option Code</th>
                                <th>Dept Option Name</th>
                                <th>Awarding Degree Name</th>
                                <th>No Of Semesters</th>
                                <th>Final Level</th>
                                <th>Action</th>
                            </tr>
                            </thead>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>



<!-- Modal -->
<div class="modal fade inmodal" id="actionsList" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <!--Content-->
        <div class="modal-content animated">
            <!--Header-->
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
                <div class="flex-center">
                    <h4 class="modal-title">DEPT OPTION</h4>
                    @*<a class="btn btn-primary btn-sm">Create new action</a>*@
                </div>
            </div>
            <!--Body-->
            <div class="modal-body" id="myModalDivBody">
            </div>
        </div>
        <!--/.Content-->
    </div>
</div>
<!-- /.Modal-->

<script src="~/Scripts/jquery-3.1.1.min.js"></script>

<script type="text/javascript">
    $(document).ready(function () {

        window.oTable = $("#Jdatatable").DataTable({
            "processing": true, // for show progress bar while loading
           
            "ajax": {
                "url": "/Programmes/GetIndex",
                "type": "POST",
                "datatype": "json"
            },
            "columns": [
                { "data": "DeptName", "autoWidth": true },
                { "data": "ProgrammeCode", "autoWidth": true },
                { "data": "ProgrammeName", "autoWidth": true },
                { "data": "AwardingDegreeName", "autoWidth": true },
                { "data": "NoOfSemesters", "autoWidth": true },
                { "data": "LevelName", "autoWidth": true },
                {
                    "data": "ProgrammeId",
                    "autoWidth": true,
                    "render": function (data) {
                        return '<a href="#" onclick="Edit(' + data + ')" >  <i class="fa fa-pencil" aria-hidden="true" >&nbsp;Edit</i></a>&nbsp;&nbsp;' +
                            '<a href="#" class="text-danger" onclick="Delete(' + data + ')" ><i class="fa fa-remove" aria-hidden="true" >&nbsp;Remove<i /></a>';
                    }
                }
            ],
            responsive: true,
            dom: '<"html5buttons"B>lTfgitp',
             lengthMenu: [
                [10, 25, 50, 100, 200, 500, 1000, 2000,3000,5000],
                [10, 25, 50, 100, 200, 500, 1000, 2000,3000,5000]
            ],
            buttons: [
                {extend: 'copy'},
                {extend: 'csv'},
                {extend: 'excel', title: 'DeptOption'},
                {extend: 'pdf', title: 'DeptOption'},

                {extend: 'print',
                    customize: function (win){
                        $(win.document.body).addClass('white-bg');
                        $(win.document.body).css('font-size', '10px');

                        $(win.document.body).find('table')
                            .addClass('compact')
                            .css('font-size', 'inherit');
                    }
                }
            ]
        });

    });
    function Edit(programmeId) {
        var url = "/Programmes/Save?id=" + programmeId;
        $('#myModalDivBody').load(url,
            function () {
                $('#actionsList').modal("show");
            });
    };
    function Delete(programmeId) {
        var url = "/Programmes/Delete?id=" + programmeId;
       
        $('#myModalDivBody').load(url,
            function () {
                $('#actionsList').modal("show");
            });
    };
    function myFunction() {
        var url = "/Programmes/Save?id=0";
    
        $('#myModalDivBody').load(url,
            function () {
                $('#actionsList').modal("show");
            });
    };
    function ExcelUpload() {
        var url = "/Programmes/UploadProgramme";
        $('#myModalDivBody').load(url,
            function () {
                $('#actionsList').modal("show");
            });
    };
</script>