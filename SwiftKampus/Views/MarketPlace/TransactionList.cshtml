
@{
    ViewBag.Title = "New Student IDCard";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid">
    <!-- First row -->
    <div class="row">
        <div class="col-md-12">
            <div class="wrapper wrapper-content animated fadeInRight">
                <div class="row">
                    <div class="col-lg-12">
                        <div class="ibox float-e-margins">
                            <div class="ibox-title">
                                <h5>C O N V O C A T I O N PICK-UPS</h5>
                                <div class="ibox-tools">
                                    <a onclick="myFunction()" class="btn btn-primary"><i class="fa fa-file-excel-o"></i> EXCEL UPLOAD</a>
                                    @*<a href="#" onclick="AddNew()" class="btn btn-primary"><i class="fa fa-user"></i>&nbsp;ADD STUDENT</a>*@
                                    @if (ViewBag.Message != null)
                                    {
                                        <div class="alert alert-success">
                                            <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
                                            <strong>@ViewBag.Message</strong>
                                        </div>
                                    }

                                    <a class="collapse-link">
                                        <i class="fa fa-chevron-up"></i>
                                    </a>
                                    <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                                        <i class="fa fa-wrench"></i>
                                    </a>
                                </div>

                                @*<a class="collapse-link">
                        <i class="fa fa-chevron-up"></i>
                    </a>
                    <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                        <i class="fa fa-wrench"></i>
                    </a>*@
                                <div class="ibox-content">
                                    <div class="card-body">
                                        <div class="row">
                                            <div class="col-md-4">
                                                <div class="form-group">
                                                    <label>Product Category</label>
                                                    @Html.DropDownList("CategoryId", (SelectList)ViewBag.CategoryId, new { @class = "form-control" })
                                                </div>
                                            </div>
                                            <div class="col-md-3">
                                                <div class="form-group">
                                                    <div>
                                                        <label>Transaction Status</label>
                                                    </div>
                                                    <select name="Status" id="Status" class="form-control">
                                                        @*<option value="null">select...</option>*@
                                                        <option value="false">Pending</option>
                                                        <option value="true">Successful</option>
                                                        @*<option value="papaya">Papaya</option>*@
                                                    </select>
                                                </div>
                                            </div>

                                            <div class="col-md-1">
                                                <br />
                                                <button type="submit" id="btn-search" class="btn btn-primary">Search/View</button>
                                            </div>
                                        </div>
                                        <br />
                                        <div class="row ">

                                        </div>
                                    </div>
                                </div>
                            </div><!--end .row -->
                        </div>
                    </div>
                    <div class="col-md-8">
                        <div class="ibox-content">
                            <div class="table-responsive">
                                <table id="Jdatatable" class="table table-striped table-bordered table-hover table-responsive dataTables-example">
                                    <thead>
                                        <tr>
                                            <th>Sn</th>
                                            <th> Customer Name</th>
                                            <th> Phone NO</th>
                                            <th> Qty</th>
                                            <th> Price</th>
                                            <th> Transact Ref</th>
                                            <th> Collected</th>
                                            <th>Actions</th>
                                        </tr>
                                    </thead>
                                </table>
                            </div>
                        </div>
                    </div>
                    <div id="partialCart" class="col-md-4">
                    </div>
                </div>
        </div>
    </div>

</div>

<!-- Modal -->
<div class="modal fade inmodal" id="actionsList" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <!--Content-->
        <div class="modal-content animated">
            <!--Header-->
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>

            </div>
            <!--Body-->
            <div class="modal-body" id="myModalDivBody">
            </div>
        </div>
        <!--/.Content-->
    </div>
</div>
<!-- /.Modal-->

<script src="~/Scripts/jquery-3.1.1.min.js"></script>

<script type="text/javascript">

    $(document).ready(function () {
        window.oTable = $("#Jdatatable").DataTable({
            "processing": true, // for show progress bar while loading
            "serverSide": true, // if it is true the preocess is done on server side
            "filter": true, // disables search box
            "info": true,

            "stateSave": true,
            "orderMulti": false, // disable multiple column loading at once
            "ajax": {
                "url": "/MarketPlace/getTransactionList",
                "type": "POST",
                "datatype": "json",
                "data": function (d) {
                    
                    d.CategoryId = $('#CategoryId').val();
                    d.Status = $('#Status').val();
                    
                }
            },
            "columns": [
                { "data": "TransactionId", "autoWidth": true, "orderable": true },
                { "data": "CustomerName", "autoWidth": true, "orderable": true },
                { "data": "Phone", "autoWidth": true, "orderable": true },
                { "data": "ProductCount", "autoWidth": true },
                { "data": "ProductPrice", "autoWidth": true },
                { "data": "TransactionReference", "autoWidth": true },
                //{ "data": "WillAttend", "autoWidth": true },
                { "data": "IssueStatus",  "autoWidth": true },
                {
                    "data": "OrderId",
                    "autoWidth": true,
                    "render": function (data) {
                        return '<a href="#" onclick="showOrderDetail(\'' + data + '\')" data-toggle="tooltip" data-placement="top" title="Enter Blood Group"><i class="fa fa-pencil" aria-hidden="true"></i>Detail</a>';
                    }
                }
            ],
            dom: '<"html5buttons"B>lTfgitp',
            buttons: [
                { extend: 'copy' },
                { extend: 'csv' },
                { extend: 'excel', title: 'Student List' },
                { extend: 'pdf', title: 'Student List' },

                {
                    extend: 'print',
                    customize: function (win) {
                        $(win.document.body).addClass('white-bg');
                        $(win.document.body).css('font-size', '10px');

                        $(win.document.body).find('table')
                            .addClass('compact')
                            .css('font-size', 'inherit');
                    }
                }
            ]
        });
        window.oTable.fnSort([[1, "asc"], [2, "desc"]]);



    });

    function getDeptList() {
        var facultyid = $("#FacultyId").val();
        $.ajax
            ({
                url: '/students/getdeptlist?codeid=' + facultyid,
                type: 'get',
                datatype: 'application/json',
                contenttype: 'application/json',
                success: function (result) {
                    $("#DepartmentId").html("");
                    $.each(JSON.parse(result),
                        function (i, city) {
                            $("#DepartmentId").append($('<option></option>').val(city.DepartmentId).html(city.DeptName));
                        });

                },
                error: function () {
                    $("#DepartmentId").html("");
                    $("#Programmeid").html("");
                }
            });
    }

    function getprogrammelist() {
        var deptid = $("#DepartmentId").val();
        $.ajax
            ({
                url: '/students/getprogrammelist?codeid=' + deptid,
                type: 'get',
                datatype: 'application/json',
                contenttype: 'application/json',
                success: function (result) {
                    $("#ProgrammeId").html("");
                    $.each(JSON.parse(result),
                        function (i, city) {
                            $("#ProgrammeId").append($('<option></option>').val(city.ProgrammeId).html(city.ProgrammeName));
                        });
                },
                error: function () {
                    $("#ProgrammeId").html("");
                }
            });

        //$("#btn-search").click(function () {
        //    var url = "/ConvocationList/getConvocationList?programmeid=" + $('#ProgrammeId').val();
        //    jQuery.noConflict();
        //    $('#Jdatatable').load(url);
        //});
    }

    $("#btn-search").click(function () {
        window.oTable.ajax.reload();

    });

    function showOrderDetail(oderId) {
        var url = "/MarketPlace/showOrderDetail?id=" + oderId;
        $('#partialCart').load(url);
    };


</script>
